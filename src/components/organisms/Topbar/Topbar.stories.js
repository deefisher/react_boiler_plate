import React from 'react';
import Topbar from './Topbar';

export const DefaultTopbar = (props) => <Topbar {...props} />;

const TopbarStory = {
    title: 'Organisms/Topbar',
    component: Topbar,
};

export default TopbarStory;
